{
  "name": "gauntlet",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build-server",
    "build-server": "rimraf ./dist/server && tsc",
    "build-client": "rimraf ./dist/client && tsc",
    "e2e:test": "npm run build && jest ./dist/modules/**/*.controller.test.js",
    "start": "npm run build && node ./dist/main.js",
    "watch": "nodemon"
  },
  "author": "UltraRangers",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^4.6.6",
    "@nestjs/core": "^4.6.6",
    "@nestjs/testing": "^4.6.6",
    "reflect-metadata": "^0.1.12",
    "rxjs": "^5.5.7",
    "typescript": "^2.7.2"
  },
  "devDependencies": {
    "@types/jest": "^22.2.0",
    "@types/node": "^9.4.7",
    "@types/supertest": "^2.0.4",
    "express": "^4.16.3",
    "jest": "^22.4.2",
    "rimraf": "^2.6.2",
    "supertest": "^3.0.0"
  }
}
